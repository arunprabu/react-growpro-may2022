// Step 1: of useContext() hook
// have the context to have data (optional) 
const { createContext } = require("react");

export const PageContext = createContext(); // you can have default value as well.

// Step 2: Refer App.js 
// Step 3: Refer HooksDemo.js  ContextDemo.js 